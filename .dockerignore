.git/
.ai-bridge/
performance/

.env
.env.*
**/.env
**/.env.*

**/.DS_Store
**/node_modules/
**/vectorstore/
**/uploads/

# Maven/build caches are not part of the Docker context. The backend runtime
# image only needs the packaged application jar.
target/
backend/target/*
!backend/target/queryweaver.jar

# Frontend Dockerfile consumes the production bundle created by npm run build.
# Keep frontend/dist in the context, but exclude transient caches.
frontend/.vite/

.idea/
.vscode/
*.iml
*.log
